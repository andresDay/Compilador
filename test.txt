DECVAR
    palabra, word : STRING ;
    cantidadLetras : INTEGER;
    letra : CHAR;
    i : INTEGER;
    variable : FLOAT;

    -/Ejemplo de declaracion de id fuera de rango/-
    -/esto_es_un_id_muy_largo: STRING;/-
ENDDEC

while (variable <12)
START

    IF ( i < 6 ) 
    START
        
        write "i es menor que 6";

        if ( i > 3 )
        START

            write "y mayor que 3";

        END

    END
end

if ( i > 3 AND i < 5) 
    START 
        WRITE "ESTOY DENTRO DEL IF ";
    END
ELSE
    START
        IF (A > 3)
            START
                WRITE "HOLA PAPU";
            END
    END    